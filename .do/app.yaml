name: ai-whatsapp-bot
region: sgp
services:
  - name: whatsapp-bot
    github:
      repo: ACCLANKA/ai-whatsapp-bot
      branch: main
      deploy_on_push: true
    source_dir: /
    environment_slug: node-js
    instance_count: 1
    instance_size_slug: basic-xxs
    http_port: 3011
    routes:
      - path: /
    envs:
      - key: PORT
        value: "3011"
      - key: NODE_ENV
        value: "production"
      - key: DISABLE_WHATSAPP
        value: "true"
      - key: SESSION_SECRET
        value: "${SESSION_SECRET}"
        type: SECRET
    run_command: node server.js
    health_check:
      http_path: /health
      initial_delay_seconds: 10
      period_seconds: 30
